# 数值求解器升级路线图(Solver Roadmap)

> 本文把求解器的分阶段升级计划**固化入仓**(此前仅存于外部计划文件),供任何接手的开发者/agent 了解
> 已完成的工作、实测验收结果、与原计划的偏差,以及后续可选阶段的验收标准。
> 开发流程与不变量见 [CONTRIBUTING.md](CONTRIBUTING.md);功能与物理模型见 [README.md](README.md)。

## 总览

主线 **阶段 1 → 2 → 3 已全部完成并发布**;阶段 4/5/6 为可选/单独评估项。
每阶段独立可发布、可验证,对应一个 release tag;所有数值改动遵循 CPU↔GPU 镜像不变量(CONTRIBUTING §3),
验收基准固化进 [`src/simulation/benchmarks.test.ts`](src/simulation/benchmarks.test.ts) 并入 CI。

| 阶段 | 主题 | 状态 | tag |
|:--:|------|:--:|:--:|
| 1 | 二阶低耗散格式(MUSCL-RK2)+ CFL 自动 dt | ✅ 完成 | v0.2.0 |
| 2 | 非线性水深 + 井平衡源 + 曼宁摩擦 + 干湿统一 | ✅ 完成 | v0.3.0 |
| 3 | 辐射边界 + 缩减纬网极地处理 | ✅ 完成 | v0.4.0 |
| 4 | 可选频散(Boussinesq) | ✅ 完成 | v0.5.0 |
| 5 | 干湿 run-up 与淹没可视化 | ⬜ 可选 | — |
| 6 | WebGPU 计算迁移 | ⬜ 单独评估 | — |

---

## ✅ 阶段 1:二阶低耗散格式(v0.2.0)

**目标**:把一阶 Lax–Friedrichs 升级为二阶低耗散格式,大幅降低行波数值耗散。

**实现**:
- MUSCL-Hancock 重构 + minmod 限制器 + **Rusanov(局部 Lax–Friedrichs)界面通量** + SSP-RK2 两步
- 时间步长自动化:`dt = CFL_SAFETY·min(dx,dy)/√(g·H_max)`,`CFL_SAFETY=0.5`,启动时按地形计算,HUD 显示实际 dt
- 保留 LF 作教学对照:`uScheme`(0=LF,1=V2),控制台「数值格式」下拉可切换
- GPU 新增中间变量(RK2 中间级);状态纹理布局 RGBA=(η, hu, hv, 1) 不变

**验收(已通过)**:
- V2 行波 1000 步幅值衰减 **<0.5%**(LF 基线约 1.5%)
- 网格减半误差 **二阶收敛**

> **与原计划偏差**:计划写的是「HLL 界面通量」,实现采用 **Rusanov 通量**(同为迎风格式,波速取 `±√(g·max(hL,hR))`,更简洁且对本问题足够)。

---

## ✅ 阶段 2:非线性水深 + 物理摩擦(v0.3.0)

**目标**:从线性浅水升级到非线性总水深,并用物理摩擦替代 ad-hoc 阻尼。

**实现**:
- 通量与波速改用总水深 `h = H+η`;动量方程 `-g·h·∇η`(**略去水平对流项**,海啸弱非线性下的教学级简化)
- **井平衡源项** `+g·η·∂h/∂x`:抵消 `g·h·η` 通量多出的地形项 → `η≡0` 时严格静水平衡(lake-at-rest)
- **隐式曼宁底摩擦** `hu /= 1 + dt·g·n²·|u|/h^(4/3)`,`n` 默认 0.025,控制台滑杆 0–0.05;**删除 `uDamping`**(V2 路径)
- **干湿统一**:`h < H_MIN=1e-3` 视干,界面通量置零、永久陆地 η 冻结/动量清零

**验收(已通过)**:
- 变地形静水平衡 `maxEta = 0`(float64 精确井平衡)
- 周期域 1 小时质量守恒 `|Σh−Σh₀|/Σh₀ = 5.7e-16`
- Green 定律浅水放大比 `(H₁/H₂)^¼` 误差 **−4.5%**(±5% 内)

---

## ✅ 阶段 3:边界与极地处理(v0.4.0)

**目标**:平面域边界从动量海绵改为辐射边界;全球域去除极地 CFL hack,改缩减纬网恢复正确波速。

**实现**:
- **平面域四边辐射边界**:边界单元投影到出射特征、令入射特征为零(`w± = η ± hu/c`,`c=√(gH)`),让出海波透射;辅以 **2 单元薄海绵兜底**吸收斜入射残余(仅 V2 路径;LF 保留旧宽海绵作教学基线)
- **全球域缩减纬网**:删除旧 `cflFloor` hack,改 stride-k 合并(`|φ|>74°` 每 2 列、`>80°` 每 4 列),有效格距 `dx_eff = k·dx·cosφ` 保持有限;globe `dt` 用缩减后最小经向格距 `min(k·cosφ)` 计算 CFL
- CPU(`kLat`/`dxEff`/`fluxX(st)`/辐射 pass)与 GPU(`STEP_FRAG` + `STEP_FRAG_V2`)同构镜像

**验收(已通过)**:
- 平面边界辐射反射能量 **<2%**(旧动量海绵约 40%)
- 赤道基准格式波速误差 **<1%**(实测 ≈0.00%,验证格式内禀波速精确)
- 74° 缩减纬网(k=2)波速误差 **<3%**(旧 `cflFloor` hack 约 73% 偏慢)
- 球面长时稳定 + 极地行 stride-k 无 NaN/发散(浏览器 WebGL 验证)

> **与原计划偏差**:计划预期「dt 50s → 约 90s,全球模拟提速」。实际结果是 **dt 变保守**(真实 1024×512 全球网格约 60.5s → 约 17.9s),
> 因为旧 `cflFloor` 通过**放大** `dx_eff` 换取了过大(且错误)的 dt;缩减纬网恢复正确波速后,稳定 dt 由真实的 `min(k·cosφ)` 决定。
> **即:阶段 3 用"更慢但正确"取代了"更快但失真"** —— 这是刻意的取舍,不是回退。缩减纬网相比"完全无保护"(`dx·cos84°≈0.1·dx`)仍显著放宽了 dt。

---

## ✅ 阶段 4:可选频散(Boussinesq,v0.5.0)

**目标**:为近场/跨洋传播提供频率频散(远场长波默认关闭)。

**实现**:
- UI 开关「频率频散(近场)」:`uDispersion`(仅 V2;LF 教学基线不支持)
- Madsen–Sørensen/Peregrine 型混合导数项的 **η 位势形式源项**:
  `S = coef·∇(∇·(−g·h·∇η))`(q_t 的 SWE 一阶迭代),在**两个 RK2 阶段内就地**求值
  (阶段一致,无跨步记忆 → 无寄生模态);只加动量 → 质量守恒与静水平衡不变
- **系数网格上限**:`coef = min(h²/3, 0.6·min(dxEff,dy)²)`——显式处理的稳定条件
  `b = coef·k² ≤ 1` 须对一切网格模式成立(kdx=π/2 时 b = coef/dx²)。
  全球网格(h/dx ≈ 0.1)上限不生效 → **完整 Peregrine 强度**(跨洋频散最重要场景);
  近场细网格(h/dx ≳ 1)按稳定上限截断
- CPU(`computeL` 频散源 `vxAt/vyAt/divAt`)与 GPU(`STEP_FRAG_V2` 的
  `dispVx/dispVy/dispDiv`)同构镜像;色散曲线 `ω² = c²k²(1 − coef·k²)`
  (Peregrine 展开形,与 Padé `1/(1+μ²/3)` 一致到 O(μ²))

**验收(已通过)**:
- μ=kh=0.8(16 pts/λ,系数上限生效):右行特征相位测得相速与实现系统解析色散曲线
  误差 **≈1.4%**;关频散对照偏离 ≈5.5%(回到无色散 ω=ck)
- 高斯波包右行 120 s:开频散 RMS 宽度扩展 **>1.1×**(频散尾波物理),质量守恒 <1e-6
- 变水深(1000→100 m 缓坡)+ 频散 3000 步稳定无发散
- `/debug.html` 64² GPU↔CPU:频散开/关两场景 L 算子差 ≤4.1e-7,20 步剖面逐位一致

> **与原计划偏差**:
> 1. 原验收「孤立波传播 10 水深后 L2 <3%」不适定:本模型按设计略去水平对流项,
>    不存在孤立波孤立子解(非线性陡化与频散无法平衡)→ 改测**色散关系相速**
>    (直接验证频散项给出的色散曲线,更贴近该项的物理作用)。
> 2. 原计划「辅助散度纹理(第二变量)」未采用:η 位势形式源项直接在步进着色器内
>    求值,无需额外纹理或额外 pass,架构更简单且 CPU/GPU 镜像更直接。
> 3. 实现中试过并否决的方案(供后人避坑):Δq=U\*\*−U⁰ 半隐式修正(慢驻波寄生,
>    ~0.2%/步能量漂移)、Δq=L⁰ 滞后修正(共振爆炸)、逐点比值限制器
>    `|S|≤0.9|L|`(|L| 过零处削顶畸变 → 谐波级联耗散)。显式 Boussinesq 的正确
>    教学级做法 = 阶段一致源项 + 网格稳定上限系数;科研级需隐式 Helmholtz 求解
>    (GeoClaw 后端补齐)。

---

## ⬜ 阶段 5:干湿 run-up 与淹没可视化

**目标**:支持真实动边界爬高与淹没范围可视化。

**计划实现**:
- **静水重构(hydrostatic reconstruction)+ 保正 HLL**:干单元保留 `h_min=1e-3` 薄膜,通量限制不使水深转负
- 新增**累计最大 run-up 纹理**(每步 max-pooling pass)+「淹没范围」彩色图层 + UI 开关

**验收标准**:
- **Thacker 抛物面碗**解析解(周期 run-up):周期/幅值误差 **<5%**;全程**无负水深**

---

## ⬜ 阶段 6:WebGPU 计算迁移(单独评估)

**目标**:突破 WebGL2 网格上限,提升大网格性能。

**计划实现**:
- 同格式 **WGSL compute** 移植,WebGL2 路径保留为 fallback;`MAX_GRID` 384 → 1024
- 仅在阶段 1–5 稳定后立项,独立分支评估

**验收标准**:
- 1024² globe 在中端 GPU **60fps**

---

## 横切:验证体系(贯穿所有阶段)

- **`src/simulation/cpuSolver.ts`**:GPU 格式的 CPU/TS 镜像(纯函数,uniform 语义一致),供 CI 无 WebGL 环境测试
- **`src/simulation/benchmarks.test.ts`**:数值基准(行波衰减、二阶收敛阶、静水平衡、质量守恒、Green 定律、辐射反射、缩减纬网极地波速、球面长时稳定、频散色散曲线、波包扩展)
- **`/debug.html`(`src/debug-gpu.ts`)**:DEV 模式 64² GPU↔CPU 逐步回读对比工具(不进 CI;频散开/关两场景)
- **CI**:Node 22,`npm ci → check → test → build`;新增基准用例随阶段进 `benchmarks.test.ts`
- 每阶段:本地 `npm run check && npm test && npm run build` + 浏览器目视验收 + 一个 release tag

## 执行顺序、风险与假设

- **顺序**:主线 1→2→3(每阶段约一次提交周期);4、5 可按需插入;6 单独评估
- **风险**:RK2 中间级 +显存;MUSCL 重构使每步纹理取样增多、单步成本约 1.5–2×(可靠"同视觉质量下降分辨率"与阶段 6 抵消)
- **回退保障**:`uScheme` 开关保留 LF 旧格式作为教学对照与回退路径
- **假设**:保持**教学定位** —— 不引入完整非线性对流项与 AMR;GeoClaw 后端继续作为高精度对照而非替代;渲染层(`SceneApp`/渲染网格分离)在数值阶段不动,仅阶段 5 计划新增淹没图层
